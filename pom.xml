<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

    <modelVersion>4.0.0</modelVersion>
    <groupId>com.rescripter</groupId>
    <artifactId>com.rescripter.parent</artifactId>
    <packaging>pom</packaging>
    <name>Rescripter Parent</name>
    <version>1.0.7-SNAPSHOT</version>

    <description>Editor and execution environment to run JavaScript that can modify Java source code.</description>
    <!-- <url>http://rescripter.com/</url> -->
    <url>http://blog.activelylazy.co.uk/2011/07/18/introduction-to-rescripter/</url>

    <prerequisites>
        <maven>3.0</maven>
    </prerequisites>

    <ciManagement>
        <system>Jenkins</system>
        <url>https://codecop.ci.cloudbees.com/job/Rescripter/</url>
    </ciManagement>

    <inceptionYear>2011</inceptionYear>

    <developers>
        <developer>
            <id>activelylazy</id>
            <name>David Green</name>
            <email>dave at activelylazy dot co dot uk</email>
            <url>http://www.activelylazy.co.uk/</url>
            <roles>
                <role>Java Developer</role>
            </roles>
        </developer>
        <developer>
            <id>peter.codecop.kofler</id>
            <name>Peter Kofler</name>
            <email>peter dot kofler at code-cop dot org</email>
            <url>http://www.code-cop.org/</url>
            <roles>
                <role>Java Developer</role>
                <role>Code Cop</role>
            </roles>
            <timezone>+1</timezone>
        </developer>
    </developers>

    <licenses>
        <license>
            <name>GNU General Public License, version 3 (GPL-3.0)</name>
            <url>http://www.opensource.org/licenses/GPL-3.0</url>
            <distribution>repo</distribution>
        </license>
    </licenses>

    <scm>
        <connection>scm:git:git://github.com/codecop/${project.artifactId}.git</connection>
        <developerConnection>scm:git:https://github.com/codecop/${project.artifactId}.git</developerConnection>
        <url>https://github.com/codecop/${project.artifactId}</url>
    </scm>

    <build>
        <sourceDirectory>src</sourceDirectory>
        <plugins>
            <plugin>
                <groupId>org.eclipse.tycho</groupId>
                <artifactId>tycho-maven-plugin</artifactId>
                <version>${tycho-version}</version>
                <extensions>true</extensions>
            </plugin>
            <plugin>
                <groupId>org.eclipse.tycho</groupId>
                <artifactId>target-platform-configuration</artifactId>
                <version>${tycho-version}</version>
                <configuration>
                    <resolver>p2</resolver>
                    <pomDependencies>consider</pomDependencies>
                </configuration>
            </plugin>
        </plugins>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.eclipse.tycho</groupId>
                    <artifactId>tycho-compiler-plugin</artifactId>
                    <version>${tycho-version}</version>
                    <configuration>
                        <compilerVersion>${project.build.java.target}</compilerVersion>
                        <encoding>${project.build.sourceEncoding}</encoding>
                        <source>${project.build.java.target}</source>
                        <target>${project.build.java.target}</target>
                    </configuration>
                </plugin>
                <!-- enable source bundle generation -->
                <plugin>
                    <groupId>org.eclipse.tycho</groupId>
                    <artifactId>tycho-source-plugin</artifactId>
                    <version>${tycho-version}</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-resources-plugin</artifactId>
                    <version>2.5</version>
                    <configuration>
                        <encoding>${project.build.sourceEncoding}</encoding>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>

    <profiles>
        <profile>
            <id>platform-galileo</id>
            <activation>
                <activeByDefault>true</activeByDefault>
            </activation>
            <properties>
                <eclipse-p2>http://download.eclipse.org/releases/galileo</eclipse-p2>
            </properties>
        </profile>
        <profile>
            <id>platform-helios</id>
            <activation>
                <property>
                    <name>platform-version-name</name>
                    <value>helios</value>
                </property>
            </activation>
        </profile>
        <profile>
            <id>platform-indigo</id>
            <activation>
                <property>
                    <name>platform-version-name</name>
                    <value>indigo</value>
                </property>
            </activation>
        </profile>
        <profile>
            <id>platform-juno</id>
            <activation>
                <property>
                    <name>platform-version-name</name>
                    <value>juno</value>
                </property>
            </activation>
        </profile>
    </profiles>

    <modules>
    	<module>com.rescripter.rhino</module>
        <module>com.rescripter.jmock</module>
        <module>com.rescripter.editor</module>
        <module>com.rescripter.editor.test</module>
	    <module>com.rescripter</module>
	    <!-- <module>com.rescripter.update-site</module> -->
    </modules>

    <repositories>
        <repository>
            <id>Eclipse</id>
            <layout>p2</layout>
            <url>${eclipse-p2}</url>
            <releases>
                <enabled>true</enabled>
            </releases>
            <snapshots>
                <enabled>true</enabled>
            </snapshots>
        </repository>
    </repositories>

    <properties>
        <tycho-version>0.15.0</tycho-version>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.build.java.target>1.5</project.build.java.target>
        <eclipse-p2>http://download.eclipse.org/releases/${platform-version-name}</eclipse-p2>
    </properties>

</project>
